---
name: monitor_redis_slave
templates:
  check_redis_slave.py: bin/check_redis_slave.py

packages: 
  - python_2.7.13

properties:
  redis_slave.host:
    description: "Slave host/ip address to run the redis slave check on"
  redis_slave.port:
    description: "Port to run the redis slave check on"
  redis_slave.password:
    description: "Password to run the redis slave check on"
